#pragma once

// ============================================================================
// AES Stub for Simulation
// ============================================================================
// Provides AES128 encryption/decryption for MeshCore.

#include <cstdint>
#include <cstddef>
#include <cstring>

// Simple AES-128 ECB implementation
// Uses lookup tables for S-box and inverse S-box

class AES128 {
public:
    static const size_t KEY_SIZE = 16;
    static const size_t BLOCK_SIZE = 16;
    
    AES128() {
        memset(round_keys_, 0, sizeof(round_keys_));
    }
    
    void setKey(const uint8_t* key, size_t len) {
        if (len < KEY_SIZE) return;
        keyExpansion(key);
    }
    
    void encryptBlock(uint8_t* output, const uint8_t* input) {
        uint8_t state[16];
        memcpy(state, input, 16);
        
        // Initial round key addition
        addRoundKey(state, 0);
        
        // Main rounds
        for (int round = 1; round < 10; round++) {
            subBytes(state);
            shiftRows(state);
            mixColumns(state);
            addRoundKey(state, round);
        }
        
        // Final round (no mixColumns)
        subBytes(state);
        shiftRows(state);
        addRoundKey(state, 10);
        
        memcpy(output, state, 16);
    }
    
    void decryptBlock(uint8_t* output, const uint8_t* input) {
        uint8_t state[16];
        memcpy(state, input, 16);
        
        // Initial round key addition
        addRoundKey(state, 10);
        
        // Main rounds
        for (int round = 9; round >= 1; round--) {
            invShiftRows(state);
            invSubBytes(state);
            addRoundKey(state, round);
            invMixColumns(state);
        }
        
        // Final round
        invShiftRows(state);
        invSubBytes(state);
        addRoundKey(state, 0);
        
        memcpy(output, state, 16);
    }

private:
    uint8_t round_keys_[176];  // 11 * 16 bytes for AES-128
    
    // AES S-box
    static const uint8_t sbox[256];
    static const uint8_t inv_sbox[256];
    
    // Rijndael round constants
    static const uint8_t rcon[11];
    
    void keyExpansion(const uint8_t* key) {
        memcpy(round_keys_, key, 16);
        
        uint8_t temp[4];
        int i = 16;
        
        while (i < 176) {
            // Copy last 4 bytes
            memcpy(temp, round_keys_ + i - 4, 4);
            
            if (i % 16 == 0) {
                // Rotate
                uint8_t t = temp[0];
                temp[0] = temp[1];
                temp[1] = temp[2];
                temp[2] = temp[3];
                temp[3] = t;
                
                // S-box
                temp[0] = sbox[temp[0]];
                temp[1] = sbox[temp[1]];
                temp[2] = sbox[temp[2]];
                temp[3] = sbox[temp[3]];
                
                // Rcon
                temp[0] ^= rcon[i / 16];
            }
            
            round_keys_[i] = round_keys_[i - 16] ^ temp[0];
            round_keys_[i + 1] = round_keys_[i - 15] ^ temp[1];
            round_keys_[i + 2] = round_keys_[i - 14] ^ temp[2];
            round_keys_[i + 3] = round_keys_[i - 13] ^ temp[3];
            
            i += 4;
        }
    }
    
    void addRoundKey(uint8_t* state, int round) {
        for (int i = 0; i < 16; i++) {
            state[i] ^= round_keys_[round * 16 + i];
        }
    }
    
    void subBytes(uint8_t* state) {
        for (int i = 0; i < 16; i++) {
            state[i] = sbox[state[i]];
        }
    }
    
    void invSubBytes(uint8_t* state) {
        for (int i = 0; i < 16; i++) {
            state[i] = inv_sbox[state[i]];
        }
    }
    
    void shiftRows(uint8_t* state) {
        uint8_t temp;
        
        // Row 1: shift left by 1
        temp = state[1];
        state[1] = state[5];
        state[5] = state[9];
        state[9] = state[13];
        state[13] = temp;
        
        // Row 2: shift left by 2
        temp = state[2];
        state[2] = state[10];
        state[10] = temp;
        temp = state[6];
        state[6] = state[14];
        state[14] = temp;
        
        // Row 3: shift left by 3
        temp = state[15];
        state[15] = state[11];
        state[11] = state[7];
        state[7] = state[3];
        state[3] = temp;
    }
    
    void invShiftRows(uint8_t* state) {
        uint8_t temp;
        
        // Row 1: shift right by 1
        temp = state[13];
        state[13] = state[9];
        state[9] = state[5];
        state[5] = state[1];
        state[1] = temp;
        
        // Row 2: shift right by 2
        temp = state[2];
        state[2] = state[10];
        state[10] = temp;
        temp = state[6];
        state[6] = state[14];
        state[14] = temp;
        
        // Row 3: shift right by 3
        temp = state[3];
        state[3] = state[7];
        state[7] = state[11];
        state[11] = state[15];
        state[15] = temp;
    }
    
    static uint8_t gmul(uint8_t a, uint8_t b) {
        uint8_t p = 0;
        for (int i = 0; i < 8; i++) {
            if (b & 1) p ^= a;
            bool hi_bit = a & 0x80;
            a <<= 1;
            if (hi_bit) a ^= 0x1b;
            b >>= 1;
        }
        return p;
    }
    
    void mixColumns(uint8_t* state) {
        for (int i = 0; i < 4; i++) {
            uint8_t a[4];
            for (int j = 0; j < 4; j++) {
                a[j] = state[i * 4 + j];
            }
            state[i * 4 + 0] = gmul(a[0], 2) ^ gmul(a[1], 3) ^ a[2] ^ a[3];
            state[i * 4 + 1] = a[0] ^ gmul(a[1], 2) ^ gmul(a[2], 3) ^ a[3];
            state[i * 4 + 2] = a[0] ^ a[1] ^ gmul(a[2], 2) ^ gmul(a[3], 3);
            state[i * 4 + 3] = gmul(a[0], 3) ^ a[1] ^ a[2] ^ gmul(a[3], 2);
        }
    }
    
    void invMixColumns(uint8_t* state) {
        for (int i = 0; i < 4; i++) {
            uint8_t a[4];
            for (int j = 0; j < 4; j++) {
                a[j] = state[i * 4 + j];
            }
            state[i * 4 + 0] = gmul(a[0], 14) ^ gmul(a[1], 11) ^ gmul(a[2], 13) ^ gmul(a[3], 9);
            state[i * 4 + 1] = gmul(a[0], 9) ^ gmul(a[1], 14) ^ gmul(a[2], 11) ^ gmul(a[3], 13);
            state[i * 4 + 2] = gmul(a[0], 13) ^ gmul(a[1], 9) ^ gmul(a[2], 14) ^ gmul(a[3], 11);
            state[i * 4 + 3] = gmul(a[0], 11) ^ gmul(a[1], 13) ^ gmul(a[2], 9) ^ gmul(a[3], 14);
        }
    }
};

// Static lookup tables - defined in cpp file
// AES S-box
inline const uint8_t AES128::sbox[256] = {
    
};

inline const uint8_t AES128::inv_sbox[256] = {
    
};

inline const uint8_t AES128::rcon[11] = {
    0x00, 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36
};
