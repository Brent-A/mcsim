[package]
name = "mcsim-companion-protocol"
version = "0.1.0"
edition = "2021"
description = "UART protocol for communicating with MeshCore companion firmware"
license = "MIT"

[dependencies]
thiserror = "2"
bytes = "1"
log = "0.4"

[dev-dependencies]
